# A01 场景入口记录

日期：2026-09-19。范围：最小灰盒场景入口；不包含 A02 移动边界、三角色验证或完整 F 目标流程。

## 状态

已完成。场景已生成、加入构建首场景，并通过 Unity 场景级验证。

## 生成尝试

- 已确认旧项目锁已释放；后续以正确引用的项目路径启动 Unity `6000.6.0f1`。
- 先排除 `-noUpm` 导致的包缺失，再使用 `-force-free` 与 UPM 正常启动 Unity 批处理。
- 执行 `CelebrationDemo.CelebrationSceneBuilder.Create` 成功，日志为 `Logs/A01-UnityBatch-ForceFreeAdmin.log`。
- 执行 `CelebrationDemo.DemoBuildValidation.ValidateScene` 成功，日志为 `Logs/A01-UnityBatch-Validate.log`，退出码为 `0`。

## 当前证据

- `Assets/Scenes/CelebrationPrototype.unity` 已生成，文件大小约 422 KB。
- `Assets/Scenes/CelebrationDemo.unity` 保留，未被覆盖。
- `ProjectSettings/EditorBuildSettings.asset` 已将 `CelebrationPrototype.unity` 置于首位。
- Builder 日志确认场景包含 3 个角色和 19 个交互目标。
- 场景验证通过：角色身份、目标数量、目标归属、引用和输入隔离均通过。

## 下一步

后续进入 A02 前，需在真实 Unity 编辑器/Play Mode 中继续验证移动边界、角色切换和最小 F 交互。A01 本阶段不再追加功能。
