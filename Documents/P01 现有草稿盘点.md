# P01 现有草稿盘点

日期：2026-09-19。盘点基线：`fd49e3d`。执行：一名 Luna xhigh 子 agent，只读盘点；主 agent 核对关键文件存在性和构建入口，整理本记录。

**P01 已验收：盘点完成。A～H 的功能仍待逐项验收。** 本轮没有修改功能代码、修复错误、启动 Unity、编译或运行测试。

## 现有草稿状态

下表的路径相对于项目根目录。可复用表示有对应源码，不能据此认定功能正常。

| 范围与任务 | 可复用草稿及入口 | 待验证内容 |
|---|---|---|
| A01～A08：输入与角色 | `Runtime/DemoRuntime.cs` 的输入、切人、交互入口；`World/ActorView.cs`、`World/FixedAngleCamera.cs`；场景生成器的角色创建 | 移动、切人、镜头、目标选择、场景引用和旧输入隔离 |
| B01～B08：单次行为与反馈 | `Core/CelebrationCore.cs` 的事件模型与执行入口；`UI/DemoHud.cs` 的 `OnAction` | 领取、购买、捐献，以及日志和两端飘字的准确性 |
| C01～C08：共享加工 | `CelebrationCore.cs` 的共享工位推进；`Editor/CoreSmokeTests.cs` 的加工检查草稿 | 单人/多人时长、迟到加入、重复加入、切人不中断、一次产出 |
| D01～D04：蛋糕表达 | `CelebrationCore.cs` 的 `CakeState`；`Editor/CelebrationSceneBuilder.cs` 的六点创建；`World/TargetView.cs` 的外观刷新 | 六点在实际场景中的位置、可达性、独立选择和外观变化 |
| E01～E05：偷吃与限时 | `CelebrationCore.cs` 的筷子配置、动作分流、偷吃和限时效果逻辑 | 工具有效期、减速叠加与共同到期、速度下限、其他交互兼容 |
| F01～F06：庆典与奖杯 | `CelebrationCore.cs` 的庆典/奖杯入口；`DemoHud.cs` 的结果展示；生成器的奖杯底座 | 参与称号、全员一次颁奖、归属、放置和重复 F |
| G01～G05：个人回顾 | `CelebrationCore.cs` 的 `GetHistory` 与奖杯回顾入口；`DemoHud.cs` 的个人回顾界面 | 本人及协作记录、超过 100 条的完整历史、滚动、关闭和切人 |
| H01～H06：体验与交付 | `DemoRuntime.cs` / `CelebrationCore.cs` 的重置；`Editor/DemoBuildValidation.cs`、`Runtime/DemoSmokeRunner.cs`、`Tools/Verify-Celebration.ps1`、`Tests/CoreSmoke` | 当前版本编译、全流程、实际画面、Windows 构建与运行入口 |

除另注外，表中脚本位于 `Assets/CelebrationDemo/` 下。旧交互骨架和旧场景也在仓库中；本轮未评估其与新草稿的逐项接口兼容性，该部分属于后续任务。

## 已确认的缺口

1. **目标场景尚不存在。** `Assets/Scenes/CelebrationPrototype.unity` 未在磁盘中找到；生成器和构建验证器指向此路径。`ProjectSettings/EditorBuildSettings.asset` 仍以 `SampleScene.unity` 为第一场景、旧 `CelebrationDemo.unity` 为第二场景。生成脚本存在，不能代替已生成并接线的场景。对应 A01、A08。
2. **Windows 构建尚不存在。** 未找到 `Builds/CelebrationDemo/CelebrationDemo.exe`。自动体验脚本需在含三角色和十九个目标的实际场景中运行，本轮没有这项运行证据。对应 H02、H05。
3. **当前草稿尚无本轮验证结果。** 检查脚本、测试项目与 UI 代码均有草稿；本轮没有执行它们，因此不能把源码或测试用例存在标为通过。对应 P03 及后续各功能验收任务。

## 历史记录与本轮证据

此前记录过一次运行时/编辑器代码独立编译通过，其后又有修改；核心规则检查曾有失败；上次 Unity 启动因许可证不可用而停止。这些是历史记录，本轮没有重测，**不据此判断许可证现在是否可用，也不声称当前版本编译通过或失败**。

最近日志显示上限为 100 条属于既定设计；个人回顾是否保留完整记录要在 G 阶段验证。本轮不把这个显示上限直接判定为历史丢失缺陷。

## 本轮结束点

- 已完成：按 A～H 盘点可复用草稿，区分缺失产物和待验证项。
- 未执行：功能开发、接口冲突核对、许可证检查、编译、测试、构建。
- 下一项建议：**P02——核对 ActorId、TargetId、事件、时间与返回结果的最小接口**。
- P02 尚未派发；完成本记录的提交后停止。
