# A03 三角色身份记录

日期：2026-09-19。范围：确认 `CelebrationPrototype` 场景中三名角色的稳定编号、出生位置、实体颜色，以及 Runtime/HUD 使用同一身份映射；不包含角色切换、镜头跟随、F 交互或 UI 美化。

## 实现

- `CelebrationSceneBuilder.BuildActors` 保持三名角色的固定顺序和出生点：1号 `(-2.3, 0, -5.8)`、2号 `(0, 0, -7.2)`、3号 `(2.3, 0, -5.8)`。
- 角色 `ActorId` 固定为 1、2、3，实体名称为 `Actor 1`、`Actor 2`、`Actor 3`，运行时引用由 `DemoRuntime.Actors` 传入。
- `ActorView.ColorForActor` 集中维护红、黄、蓝颜色映射；场景生成器使用它创建实体材质，HUD 使用它渲染角色卡片、日志和反馈颜色。
- 场景验证新增角色引用、数组顺序、名称、头顶锚点、选中标记、出生点间距和实体材质颜色检查。

## 验证

- 静态场景检查确认存在 3 个 ActorView，编号为 1/2/3，出生点互不重叠，Body 材质分别绑定 Red/Yellow/Blue，Runtime/HUD 引用已接线。
- `Tools/Verify-Celebration.ps1`：通过，输出 `CELEBRATION_COMPILE_PASS` 与 `CELEBRATION_CORE_PASS: 66`。
- 场景静态检查：通过，输出 `A03_STATIC_SCENE_PASS`，确认 ActorId 1/2/3、三处分离出生点和 Red/Yellow/Blue 材质。
- Unity Play Mode：仍受已有 Unity 实例项目锁阻塞，未强制关闭现有进程。

## 后续建议

释放项目锁后运行一次 `Validate Prototype Scene`，再继续后续阶段；A03 不扩展角色切换或交互逻辑。
