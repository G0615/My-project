using UnityEngine;

// Scene construction belongs in the editor setup script. Runtime gameplay
// uses the Player, environment and UI objects already saved in the scene.
// This class is intentionally empty and kept only so existing references do
// not break if an older scene or script list still mentions the type.
public static class DemoRuntimeBootstrap
{
}
